import { createContext } from 'react'
const RevistaContext	= createContext(null) 
//Dentro de ese contexto se meten variables globales que se distribuyen a todos los componentes
export default RevistaContext